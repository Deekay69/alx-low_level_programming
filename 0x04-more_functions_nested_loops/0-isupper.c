#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character.
 *
 * Return: 0
 */
int _isupper(int c)
{
	if (c = 'A')
	{
		return (1);
	}
	else if (c = 'a')
	{
		return (0);
	}
}
