#include "main.h"
#include <stdio.h>

/**
 * mul -  multiplies two integers
 * @a: interger to be multipied
 * @b: interger to be multiplied
 * Return: Always success
 */

int mul(int a, int b)
{
	return (a * b);
}
